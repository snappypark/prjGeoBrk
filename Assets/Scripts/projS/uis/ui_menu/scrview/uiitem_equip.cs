using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;
using Mosframe;

public class uiitem_equip : UIBehaviour, IDynamicScrollViewItem
{
    public void onUpdateItem(int index, int totlaIdx)
    {
     //   _idx = index;
     //   _text.text = string.Format("{0}", (index + 1 - gapIdx));

    }
}
